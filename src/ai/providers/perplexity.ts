/**
 * Perplexity provider configuration.
 * Uses the standard OpenAI-compatible base class — no overrides needed.
 * Default: baseUrl = https://api.perplexity.ai, model = sonar
 */
export { OpenAICompatibleService as PerplexityService } from './openai-compatible';

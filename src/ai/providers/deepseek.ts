/**
 * DeepSeek provider configuration.
 * Uses the standard OpenAI-compatible base class — no overrides needed.
 * Default: baseUrl = https://api.deepseek.com, model = deepseek-chat
 */
export { OpenAICompatibleService as DeepSeekService } from './openai-compatible';

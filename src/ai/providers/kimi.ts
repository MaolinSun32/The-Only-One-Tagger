/**
 * Kimi (Moonshot) provider configuration.
 * Uses the standard OpenAI-compatible base class — no overrides needed.
 * Default: baseUrl = https://api.moonshot.cn, model = moonshot-v1-8k
 */
export { OpenAICompatibleService as KimiService } from './openai-compatible';

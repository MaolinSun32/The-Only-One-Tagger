/**
 * Qwen provider configuration.
 * Uses the standard OpenAI-compatible base class — no overrides needed.
 * Default: baseUrl = https://dashscope.aliyuncs.com/compatible-mode, model = qwen-plus
 */
export { OpenAICompatibleService as QwenService } from './openai-compatible';
